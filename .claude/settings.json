{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); if echo \"$input\" | grep -qE '\\btsc\\b'; then echo '{\"decision\":\"block\",\"reason\":\"Do not run tsc directly. Use `pnpm check` instead.\"}'; fi"
          }
        ]
      }
    ]
  }
}
